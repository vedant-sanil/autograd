#include <iostream> 

class Tensor {
    /**
     * Creates a Tensor object, a container for data
     * and supporting operations in an Autograd system.
     * Contains arguments that enable packaging of data
     * with functional operations.
     */
    public:
        Tensor()
};